import React from 'react';
import Onboarding from './View/Onboarding';

const index = ({str}) => {
    return (
        <>
            <Onboarding str={str}/>
        </>
    );
}

export default index;
